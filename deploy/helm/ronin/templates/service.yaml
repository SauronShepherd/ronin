apiVersion: v1
kind: Service
metadata:
  name: {{ include "ronin.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "ronin.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
