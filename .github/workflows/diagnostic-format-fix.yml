name: Diagnostic format fix

on:
  push:
    branches:
      - automation/diagnostic-contracts

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: automation/diagnostic-contracts
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install development dependencies
        run: python -m pip install -e '.[dev]'
      - name: Apply repository formatter
        run: ruff format .
      - name: Commit formatter output
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add python/studio_core/diagnostics.py tests/test_diagnostics.py
          git commit -m 'style: apply diagnostic formatting'
          git push origin HEAD:automation/diagnostic-contracts
