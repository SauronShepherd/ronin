"""Pure domain primitives for Ronin Studio.

The package must remain free of filesystem, network, subprocess, database and
environment access. The rule is enforced by ``tools/architecture_gate.py``.
"""

__all__: tuple[str, ...] = ()
