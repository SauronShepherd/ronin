# Prototype disposition

The production UI keeps the prototype's information architecture and visual
language, but removes invented provider candidates and connector data.

Backend-backed screens in the first slice are Runs, Run detail, Workspaces,
Governance/Access and SQL. The remaining surfaces stay visible as disabled
planned capabilities; they do not display mock data.
