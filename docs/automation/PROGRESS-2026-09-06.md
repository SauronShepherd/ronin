# Ronin progress supplement — 2026-09-06

This dated supplement records the current durable-execution milestone without rewriting the long historical publication log in `PROGRESS.md`. `docs/product/V01_SCOPE.md` remains the scope authority, and `BACKLOG.md` / `CONSTRUCTION_PLAN.md` remain the current selection order.

## Real Docker durable worker milestones

- PR #135 qualified the canonical five-code-cell demo through `LocalWorkerRuntime -> SQLite -> DockerContainerKernelExecutor` using the real Docker command path. The demo is deliberately self-contained per cell, so clean containers do not depend on interpreter memory or container-local `/tmp` surviving between cells.
- PR #137 added a continuous local worker lifecycle with interruptible idle waits, unique Attempt and lease identities per poll, signal-driven graceful shutdown, immediate fenced abandonment of an active claim, and Attempt-limit continuity.
- PR #138 adds the missing abrupt-process qualification. A separate worker process executes and durably checkpoints three cells through real Docker, is terminated non-gracefully before the fourth Docker launch, waits through the production 30-second lease TTL, and is replaced by a second process over the same SQLite/artifact state. The replacement reclaims the same logical Run, verifies and reuses exactly the three completed cells, executes only the two missing cells, and reaches one successful terminal state.

## Qualification evidence for PR #138 head before documentation synchronization

The implementation-only head `203c2c78562c72d69e984b1ee04d07c0386e74ad` established the behavior before this documentation synchronization:

- CI run `34061583184`: success.
- Security qualification run `34061583194`: success.
- Docker Qualification run `34061583187`: success.
- Docker Qualification executed both the hardened-container qualification and durable-worker real-Docker integration: `4 passed in 39.17s`.
- The qualification image ID was `sha256:b8fe4ce3655e95f7f22c2a87d8e03a2f1f0cedc488a8e9adf18cc5a18cfdf401`, bootstrapped from repository digest `python@sha256:9534e5a8e315485d4061ed659af0fd78a284c015f9b73661b41d6bab25604534`.
- Existing Docker cleanup qualification remained green with zero residual containers after cancellation and timeout.

Because this progress supplement changes the PR head, the final merge still requires a fresh exact-head Actions pass after all documentation changes. The evidence above proves the behavior discovered during the slice; it is not substituted for the final exact-head merge gate.

## Handoff status

Issue #57 remains open. Its crash/reclaim requirement is materially advanced, but the issue must not be closed until the frozen fifteen-step v0.1 journey actually executes and passes, including HTTP/SDK, idempotency, logs/evidence, supported cancellation, CLI/operator and Compose steps.

The next critical slice is #125: qualify bounded storage/artifact contention at the real server and worker runtime call sites before the HTTP/OpenAPI/SDK surface hardens those budgets.
