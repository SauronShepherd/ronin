# Ronin v0.1 progress — 2026-09-07

## Worker acceptance activation

Builder slice: activate only frozen acceptance steps 6-9 against the already-landed durable worker crash/reclaim/resume path.

Starting `main`: `4714bee8cedc545d1b6858078aff583c49841091`.

The acceptance journey now exercises steps 6-9 in the dedicated real-Docker qualification environment. One module-scoped journey starts a separate worker OS process, allows three durable code-cell checkpoints, kills that worker non-gracefully, preserves the production 30-second lease contract, starts a replacement worker after expiry, reclaims the same logical Run, reuses exactly the three verified checkpoints, executes only the remaining two code cells, and reaches one successful terminal worker outcome.

The ordinary CI acceptance-progress job may still report these tests skipped when the dedicated real-Docker environment is absent. `Docker Qualification` is the authoritative progress evidence for these four steps and emits acceptance-gate evidence for the exact PR/main SHA.

Step 12 remains skipped. Durable cell evidence exists internally, but the frozen contract requires the supported `ronin evidence J` public/operator path. Internal store inspection is not substituted for that missing boundary.

No product code, HTTP surface, CLI implementation, storage semantics, lease duration, durability gate, security gate, coverage threshold, or release gate is weakened by this slice.

## Current acceptance position after this slice

- Live in dedicated real-Docker acceptance qualification: steps 6, 7, 8, 9.
- Still blocked by missing supported surfaces: steps 1-5 and 10-15.
- Strict release acceptance remains incomplete until all fifteen frozen steps execute and pass with zero skipped/xfail/failed/error/missing/unexpected outcomes.

## Next recommended slice

Follow the 2026-09-07 06:40 CEST build plan unless newer `main` evidence supersedes it: expose the already-qualified machinery through the bounded `ronin` CLI/operator surface, while separately retaining P1 architecture/release findings (#139, #123, #95) for their ordered slices and not opening them in parallel.
