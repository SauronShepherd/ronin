# Baseline — 2026-09-05

Commit: `a0af31717fb4252010caf54cc5790c992879746e`
Branch: `chore/v01-scope-freeze`
Python: `3.11.16`
Platform: `Linux 6.17.0-1022-azure x86_64` on GitHub-hosted Ubuntu 24.04

The day plan named historical base `e275b3a`, but `main` had already advanced through repository security qualification and the async-cancellation terminal-evidence fix. This baseline therefore starts from the actual current `main` SHA above so later measurements do not silently exclude already-published fixes.

Measurements were collected on temporary measurement commit `3d3823fbf91d67a887be4679a527781105ff5372`, whose only delta from the baseline commit was the disposable GitHub Actions measurement workflow. That workflow was removed immediately after the successful run.

Baseline evidence: GitHub Actions run `33954778796` (`Phase 1 baseline capture`). The fresh virtualenv contained 29 frozen distributions; the exact `pip freeze` output was preserved as workflow artifact `9965987949` for use as the phase-7 lockfile seed.

## Gate timings

| Stage | Seconds |
|---|---:|
| format | 0.025 |
| lint | 0.026 |
| typecheck | 1.268 |
| architecture | 0.097 |
| gates-negative | 0.047 |
| test | 6.306 |

`make check` was green in the same fresh Python 3.11.16 virtualenv after all six individually timed stages passed.

## Coverage

| Package | Line | Branch |
|---|---:|---:|
| `studio_core` | 100.00% | 100.00% |
| `studio_notebook` | 100.00% | 100.00% |
| `studio_kernel` | 100.00% | 100.00% |
| `studio_runners` | 100.00% | 100.00% |

## Performance

| Benchmark | Value |
|---|---:|
| `Pipeline` construction, 800-node fan-in graph | 12.470 ms |
| `Node.param` × 100,000 on a 50-parameter node | 153.620 ms |
| `validate_operator_node` × 20,000 against builtin catalog | 149.782 ms |
| `JsonlExecutionEventSink.append` × 200 on `/dev/shm` | 0.017726 ms/event |

The plan's comparison numbers were used only as orientation. The recorded values above are the actual runner measurements. In particular, operator validation measured substantially slower than the plan's ~57 ms reference and is intentionally recorded without adjustment.

## Mutation starting point

Mutation was not rerun for Phase 1, per the day plan. The last repository-qualified `studio_core` evidence remains:

- 1,899 killed
- 208 survived
- 2,107 total
- 90.13% score
- zero invalid-evidence categories

## Known open defects at baseline

The day plan carried review #2 findings N1–N13 plus residual B6, B8, B14, B15 and performance items P1–P4. Because this baseline uses current `main` rather than historical `e275b3a`, N1 is already resolved by PR #67 / commit `a0af31717fb4252010caf54cc5790c992879746e` and is not an open baseline defect.

Still carried for independent validation or implementation:

- N2–N13
- residual B6, B8, B14, B15
- performance items P1–P4

This list records review-carried work, not a claim that every item was independently revalidated during Phase 1.

## Phase 1 acceptance

- [x] Branch `chore/v01-scope-freeze` exists from the actual current `main` baseline `a0af31717fb4252010caf54cc5790c992879746e`.
- [x] A fresh Python 3.11.16 virtualenv installed `.[dev]` and `make check` passed.
- [x] Every baseline table cell contains a measured value; no placeholder remains.
- [x] Dependency freeze seed retained as workflow artifact `9965987949`.
