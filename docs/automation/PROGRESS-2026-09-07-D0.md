# Ronin progress — 2026-09-07 D0 local CLI

## Scope

This increment completes the HTTP-independent D0 CLI foundation selected by the synchronized v0.1 construction plan.

## Implemented

- Enabled the installed `ronin` console entry point through `studio_cli:main`.
- Added `ronin doctor` for Python 3.11+, Git and Docker executable prerequisites.
- Added `ronin validate <project>` using the canonical `ProjectManifest`, `NotebookDocument` and notebook dependency analysis contracts.
- Added `ronin plan <project> -t <target>` using the canonical deterministic execution order and dependency levels.
- Added fail-closed path handling for absolute/escaping/malformed targets and invalid project/notebook inputs.
- Added direct CLI tests plus frozen acceptance execution through the installed `ronin` command.
- Activated frozen v0.1 acceptance steps 2, 3 and 4 and removed them from the exact Docker Qualification skip allowance. The allowance now names only steps 1, 5 and 10-15.

## Boundaries preserved

- No HTTP/server behavior was added.
- No provider-specific runtime, engine, cloud or catalog semantics entered canonical domain packages.
- CLI behavior composes existing core/notebook contracts instead of duplicating validation or planning semantics.
- Existing worker crash/reclaim/resume acceptance steps 6-9 are unchanged.
- Coverage, architecture, security, durability and qualification thresholds are unchanged.

## Release impact

After this increment, seven frozen acceptance steps are intended to be live in the Docker-capable qualification context: 2, 3, 4, 6, 7, 8 and 9. Eight steps remain explicitly skipped until their backing product surfaces land: 1, 5, 10, 11, 12, 13, 14 and 15.

Authoritative completion evidence is the exact-head pull-request CI plus exact published-main workflows after merge; this file does not claim those results before GitHub Actions reports them.
