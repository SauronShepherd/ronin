# Ronin progress — 2026-09-07 S1 doctor requirement scoping

## Scope

This slice corrects a host-dependence regression in frozen acceptance step 2 without weakening the user-facing diagnostic.

`ronin doctor` remains the strict default and requires every implemented check. `ronin doctor --require=all` is the explicit equivalent. `ronin doctor --require=core` requires the currently implemented host-independent core checks (Python >=3.11 and Git) while still executing and printing the Docker check and its explicit verdict.

The implementation deliberately does not invent workspace/data/database checks that do not yet exist in `studio_cli`.

## Acceptance impact

Frozen step 2 invokes `ronin doctor --require=core`, asserts the required core checks succeed, and also asserts the Docker check is present in output. Docker absence therefore remains visible but no longer turns the acceptance suite red on a host where containers are not required for this specific diagnostic step.

The strict default remains fail-closed when Docker is absent. No skip, xfail, generic allowance, durability gate, security gate, coverage gate, or Docker qualification requirement is weakened.

Acceptance step count is unchanged: steps 2,3,4,6,7,8,9 remain live; steps 1,5,10,11,12,13,14,15 remain skipped until their supported product paths land.

## Planning impact

The next coherent slice is the storage implementation collapse identified by the 2026-09-07 13:55 CEST build-plan update: replace shadowed pagination/event SQL in the canonical store implementation, remove repeated same-named subclass overrides, and add a defensible per-file coverage floor without changing the already-correct exported behavior. After that, prioritize the high-yield network/operator CLI slice over already-landed HTTP endpoints.

#53 remains open after the portable evidence identity increment; public `/evidence` still waits for its remaining durable/public mapping work.
